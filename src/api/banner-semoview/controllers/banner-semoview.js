'use strict';

/**
 * banner-semoview controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::banner-semoview.banner-semoview');
